# Setup the project with following commands on terminal

npm init
npm i nodemon
npm i express
npm i mongoose
npm i dotenv

# Within index.js connected the projet to the server.

# Created to-do-database on mongoDb and extracted connection url.

# Created .env file and added the url

# Created database connection file and connected it with the server.

# Created router file for To-Do and connected it through index.js.

# Created models for to-do.

# Created Controllers for to-do.

# Connected models file with the controllers and added logic for all the APIs.

# To run the To-Do App. Below commands need to be run on the system.

1. npm install
2. npm run dev